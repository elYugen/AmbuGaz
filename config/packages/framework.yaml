# config/packages/framework.yaml
framework:
    secret: '%env(APP_SECRET)%'
    
    # Protection CSRF normale (avec sessions)
    csrf_protection: true
    
    # Configuration des sessions
    session:
        handler_id: null
        cookie_secure: auto
        cookie_httponly: true
        cookie_samesite: lax
    
