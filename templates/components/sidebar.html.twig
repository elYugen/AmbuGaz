<aside class="fixed left-0 top-0 h-screen w-64 bg-slate-900 text-white shadow-xl">
    <!-- Logo / Header -->
    <div class="flex items-center justify-center h-16 border-b border-slate-700">
        <span class="text-xl font-bold text-emerald-400">AmbuGaz</span>
    </div>

    <!-- Navigation -->
    <nav class="mt-6">
        <ul class="space-y-2 px-4">
            <!-- Accueil -->
            <li>
                <a href="{{ path('app_auth') }}" 
                   class="flex items-center gap-3 px-4 py-3 rounded-lg transition-all duration-200 
                          hover:bg-slate-800 hover:text-emerald-400
                          {{ app.request.attributes.get('_route') == 'app_auth' ? 'bg-slate-800 text-emerald-400 border-l-4 border-emerald-400' : 'text-slate-300' }}">
                    <!-- Icône Home -->
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                              d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
                    </svg>
                    <span class="font-medium">Accueil</span>
                </a>
            </li>

            <!-- Vide -->
            <li>
                <a href="#" 
                   class="flex items-center gap-3 px-4 py-3 rounded-lg transition-all duration-200 
                          hover:bg-slate-800 hover:text-emerald-400 text-slate-300">
                    <!-- Icône Folder/Empty -->
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" 
                              d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-6l-2-2H5a2 2 0 00-2 2z"/>
                    </svg>
                    <span class="font-medium">Vide</span>
                </a>
            </li>
        </ul>
    </nav>
</aside>
