{% extends 'base.html.twig' %}

{% block title %}Tableau de bord - AmbuGaz{% endblock %}

{% block body %}
<div class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100 py-12 px-4">
    <div class="max-w-6xl mx-auto">
        <!-- Welcome Card -->
        <div class="bg-white rounded-2xl shadow-xl p-8 mb-8">
            <div class="flex justify-between items-start mb-6">
                <div>
                    <h1 class="text-4xl font-bold text-gray-800 mb-2">
                        Bienvenue, {{ app.user.firstName }} ! 👋
                    </h1>
                    <p class="text-gray-600">
                        Connecté en tant que <span class="font-semibold">{{ app.user.email }}</span>
                    </p>
                </div>
                
                <!-- Bouton de déconnexion -->
                <a href="{{ path('app_logout') }}" 
                   class="flex items-center gap-2 bg-red-500 text-white px-6 py-3 rounded-lg hover:bg-red-600 transition duration-200 shadow-md hover:shadow-lg">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
                    </svg>
                    Déconnexion
                </a>
            </div>

            <!-- Info rapides -->
            <div class="grid md:grid-cols-3 gap-6 mt-8">
                <!-- Profil -->
                <div class="bg-gradient-to-br from-blue-500 to-blue-600 rounded-xl p-6 text-white shadow-lg">
                    <div class="flex items-center gap-3 mb-3">
                        <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/>
                        </svg>
                        <h3 class="text-xl font-bold">Profil</h3>
                    </div>
                    <p class="text-blue-100 text-lg">{{ app.user.firstName }} {{ app.user.lastName }}</p>
                    <p class="text-blue-100 text-sm mt-2">📧 {{ app.user.email }}</p>
                    <p class="text-blue-100 text-sm mt-1">📱 {{ app.user.phone }}</p>
                </div>

                <!-- Entreprise -->
                <div class="bg-gradient-to-br from-green-500 to-green-600 rounded-xl p-6 text-white shadow-lg">
                    <div class="flex items-center gap-3 mb-3">
                        <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/>
                        </svg>
                        <h3 class="text-xl font-bold">Entreprise</h3>
                    </div>
                    {# 👇 Utilise companyId au lieu de company #}
                    {% if app.user.companyId %}
                        <p class="text-green-100 text-lg font-semibold">{{ app.user.companyId.name }}</p>
                    {% else %}
                        <p class="text-green-100">Non définie</p>
                    {% endif %}
                </div>

                <!-- Statut -->
                <div class="bg-gradient-to-br from-purple-500 to-purple-600 rounded-xl p-6 text-white shadow-lg">
                    <div class="flex items-center gap-3 mb-3">
                        <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                        </svg>
                        <h3 class="text-xl font-bold">Statut</h3>
                    </div>
                    <div class="flex items-center gap-2">
                        <div class="w-3 h-3 bg-green-300 rounded-full animate-pulse"></div>
                        <p class="text-purple-100 font-semibold text-lg">En ligne</p>
                    </div>
                    <p class="text-purple-100 text-sm mt-2">Session active</p>
                </div>
            </div>
        </div>

        <!-- Actions rapides -->
        <div class="grid md:grid-cols-2 gap-6">
            <div class="bg-white rounded-xl shadow-lg p-6 hover:shadow-xl transition">
                <h3 class="text-xl font-bold text-gray-800 mb-4">Actions rapides</h3>
                <div class="space-y-3">
                    <a href="#" class="flex items-center gap-3 text-gray-700 hover:text-blue-600 transition">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
                        </svg>
                        Nouvelle commande
                    </a>
                    <a href="#" class="flex items-center gap-3 text-gray-700 hover:text-blue-600 transition">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"/>
                        </svg>
                        Historique
                    </a>
                    <a href="{{ path('app_logout') }}" class="flex items-center gap-3 text-red-600 hover:text-red-700 transition font-semibold">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
                        </svg>
                        Déconnexion
                    </a>
                </div>
            </div>

            <div class="bg-white rounded-xl shadow-lg p-6 hover:shadow-xl transition">
                <h3 class="text-xl font-bold text-gray-800 mb-4">Informations système</h3>
                <div class="space-y-2 text-sm text-gray-600">
                    <p>✅ Session active</p>
                    <p>🔐 Authentifié</p>
                    <p>📍 ID Utilisateur : <span class="font-mono">{{ app.user.id }}</span></p>
                    <p>🏢 ID Entreprise : <span class="font-mono">{{ app.user.companyId ? app.user.companyId.id : 'N/A' }}</span></p>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
