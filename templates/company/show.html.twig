{% extends 'base.html.twig' %}

{% block title %}{{ company.name }} - Détails{% endblock %}

{% block body %}
<div class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100 py-8 px-4">
    <div class="max-w-4xl mx-auto">
        <!-- Header avec retour -->
        <div class="mb-6">
            <a href="{{ path('app_company_index') }}" 
               class="inline-flex items-center gap-2 text-gray-600 hover:text-gray-800 transition">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
                </svg>
                Retour à la liste
            </a>
        </div>

        <!-- Card principale -->
        <div class="bg-white rounded-2xl shadow-xl overflow-hidden">
            <!-- Header de la card -->
            <div class="bg-gradient-to-r from-blue-600 to-blue-700 px-8 py-6">
                <div class="flex items-center gap-4">
                    <div class="h-16 w-16 bg-white rounded-xl flex items-center justify-center shadow-lg">
                        <span class="text-blue-600 font-bold text-2xl">{{ company.name|first|upper }}</span>
                    </div>
                    <div>
                        <h1 class="text-3xl font-bold text-white mb-1">{{ company.name }}</h1>
                        <p class="text-blue-100 text-sm">Détails de l'entreprise</p>
                    </div>
                </div>
            </div>

            <!-- Contenu détaillé -->
            <div class="p-8">
                <div class="grid md:grid-cols-2 gap-6">
                    <!-- ID -->
                    <div class="bg-gray-50 rounded-xl p-5 border border-gray-200">
                        <div class="flex items-center gap-3 mb-2">
                            <div class="h-10 w-10 bg-purple-100 rounded-lg flex items-center justify-center">
                                <svg class="w-5 h-5 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 20l4-16m2 16l4-16M6 9h14M4 15h14"/>
                                </svg>
                            </div>
                            <h3 class="text-sm font-semibold text-gray-500 uppercase tracking-wider">Identifiant</h3>
                        </div>
                        <p class="text-2xl font-bold text-gray-800 font-mono">#{{ company.id }}</p>
                    </div>

                    <!-- Nom -->
                    <div class="bg-gray-50 rounded-xl p-5 border border-gray-200">
                        <div class="flex items-center gap-3 mb-2">
                            <div class="h-10 w-10 bg-blue-100 rounded-lg flex items-center justify-center">
                                <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/>
                                </svg>
                            </div>
                            <h3 class="text-sm font-semibold text-gray-500 uppercase tracking-wider">Nom</h3>
                        </div>
                        <p class="text-2xl font-bold text-gray-800">{{ company.name }}</p>
                    </div>

                    <!-- Adresse -->
                    <div class="bg-gray-50 rounded-xl p-5 border border-gray-200 md:col-span-2">
                        <div class="flex items-center gap-3 mb-2">
                            <div class="h-10 w-10 bg-green-100 rounded-lg flex items-center justify-center">
                                <svg class="w-5 h-5 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
                                </svg>
                            </div>
                            <h3 class="text-sm font-semibold text-gray-500 uppercase tracking-wider">Adresse</h3>
                        </div>
                        <p class="text-lg text-gray-800">{{ company.address|default('Non renseignée') }}</p>
                    </div>

                    <!-- Téléphone -->
                    <div class="bg-gray-50 rounded-xl p-5 border border-gray-200">
                        <div class="flex items-center gap-3 mb-2">
                            <div class="h-10 w-10 bg-orange-100 rounded-lg flex items-center justify-center">
                                <svg class="w-5 h-5 text-orange-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
                                </svg>
                            </div>
                            <h3 class="text-sm font-semibold text-gray-500 uppercase tracking-wider">Téléphone</h3>
                        </div>
                        <p class="text-lg text-gray-800">{{ company.phone|default('Non renseigné') }}</p>
                    </div>

                    <!-- Email -->
                    <div class="bg-gray-50 rounded-xl p-5 border border-gray-200">
                        <div class="flex items-center gap-3 mb-2">
                            <div class="h-10 w-10 bg-red-100 rounded-lg flex items-center justify-center">
                                <svg class="w-5 h-5 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
                                </svg>
                            </div>
                            <h3 class="text-sm font-semibold text-gray-500 uppercase tracking-wider">Email</h3>
                        </div>
                        <p class="text-lg text-gray-800 break-all">{{ company.mail|default('Non renseigné') }}</p>
                    </div>

                    <!-- SIRET -->
                    <div class="bg-gray-50 rounded-xl p-5 border border-gray-200 md:col-span-2">
                        <div class="flex items-center gap-3 mb-2">
                            <div class="h-10 w-10 bg-indigo-100 rounded-lg flex items-center justify-center">
                                <svg class="w-5 h-5 text-indigo-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
                                </svg>
                            </div>
                            <h3 class="text-sm font-semibold text-gray-500 uppercase tracking-wider">SIRET</h3>
                        </div>
                        <p class="text-lg text-gray-800 font-mono">{{ company.siret|default('Non renseigné') }}</p>
                    </div>
                </div>
            </div>

            <!-- Footer avec actions -->
            <div class="bg-gray-50 px-8 py-4 border-t border-gray-200">
                <div class="flex items-center justify-between">
                    <a href="{{ path('app_company_index') }}" 
                       class="inline-flex items-center gap-2 text-gray-600 hover:text-gray-800 font-medium transition">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
                        </svg>
                        Retour
                    </a>
                    
                    {% if is_granted('ROLE_DEV') or (is_granted('ROLE_ADMIN') and app.user.company and app.user.company.id == company.id) %}
                        <div class="flex gap-3">
                            <a href="{{ path('app_company_edit', {'id': company.id}) }}" 
                               class="inline-flex items-center gap-2 bg-blue-600 hover:bg-blue-700 text-white px-5 py-2 rounded-lg transition shadow-md">
                                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"/>
                                </svg>
                                Modifier
                            </a>
                            
                            {% if is_granted('ROLE_DEV') %}
                                <button onclick="toggleDeleteModal()" 
                                        class="inline-flex items-center gap-2 bg-red-600 hover:bg-red-700 text-white px-5 py-2 rounded-lg transition shadow-md">
                                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/>
                                    </svg>
                                    Supprimer
                                </button>
                            {% endif %}
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>

        <!-- Info card pour non-dev et non-admin de cette entreprise -->
        {% if not is_granted('ROLE_DEV') and not (is_granted('ROLE_ADMIN') and app.user.company and app.user.company.id == company.id) %}
            <div class="mt-6 bg-blue-50 border-l-4 border-blue-500 rounded-lg p-4">
                <div class="flex">
                    <svg class="w-5 h-5 text-blue-500 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                    </svg>
                    <div class="ml-3">
                        <p class="text-sm text-blue-700">
                            <span class="font-semibold">Mode consultation :</span> Vous consultez les informations de votre entreprise. Pour toute modification, contactez un développeur.
                        </p>
                    </div>
                </div>
            </div>
        {% endif %}
    </div>
</div>

<!-- Modal de confirmation de suppression -->
{% if is_granted('ROLE_DEV') %}
<div id="deleteModal" class="hidden fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center p-4">
    <div class="bg-white rounded-2xl shadow-2xl max-w-md w-full p-6">
        <div class="flex items-center gap-4 mb-4">
            <div class="h-12 w-12 bg-red-100 rounded-full flex items-center justify-center">
                <svg class="w-6 h-6 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
                </svg>
            </div>
            <div>
                <h3 class="text-xl font-bold text-gray-900">Confirmer la suppression</h3>
                <p class="text-sm text-gray-500 mt-1">Cette action est irréversible</p>
            </div>
        </div>
        
        <p class="text-gray-700 mb-6">
            Êtes-vous sûr de vouloir supprimer l'entreprise <span class="font-semibold">{{ company.name }}</span> ? Toutes les données associées seront définitivement perdues.
        </p>
        
        <div class="flex gap-3">
            <button onclick="toggleDeleteModal()" 
                    class="flex-1 bg-gray-200 hover:bg-gray-300 text-gray-800 px-4 py-2 rounded-lg transition font-medium">
                Annuler
            </button>
            
            <form method="post" action="{{ path('app_company_delete', {'id': company.id}) }}" class="flex-1">
                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ company.id) }}">
                <button type="submit" 
                        class="w-full bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-lg transition font-medium">
                    Supprimer définitivement
                </button>
            </form>
        </div>
    </div>
</div>

<script>
function toggleDeleteModal() {
    const modal = document.getElementById('deleteModal');
    modal.classList.toggle('hidden');
}

// Fermer la modal en cliquant en dehors
document.getElementById('deleteModal')?.addEventListener('click', function(e) {
    if (e.target === this) {
        toggleDeleteModal();
    }
});
</script>
{% endif %}
{% endblock %}