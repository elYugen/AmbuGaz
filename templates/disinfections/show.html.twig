{% extends 'base.html.twig' %}

{% block title %}Disinfections{% endblock %}

{% block body %}
    <h1>Disinfections</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ disinfection.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ disinfection.date ? disinfection.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DisinfectionType</th>
                <td>{{ disinfection.disinfectionType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_disinfections_index') }}">back to list</a>

    <a href="{{ path('app_disinfections_edit', {'id': disinfection.id}) }}">edit</a>

    {{ include('disinfections/_delete_form.html.twig') }}
{% endblock %}
